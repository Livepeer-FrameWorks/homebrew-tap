# Homebrew Tap for FrameWorks

## Usage

```bash
brew tap livepeer-frameworks/tap

# CLI
brew install frameworks-cli

# Desktop app (macOS, once available)
brew install --cask frameworks
```

## Auto-update

Formulas and casks are automatically bumped by the monorepo's `release.yml` workflow on each tagged release via `scripts/bump.sh`.
